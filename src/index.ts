console.log('Welcome to Strict Class');

const fn = () => 9;

const username = 'Juanjo';
const usernameType = typeof username === 'string';

for (const index of [1, 2, 3, 4]) {
    console.log(index);
}
